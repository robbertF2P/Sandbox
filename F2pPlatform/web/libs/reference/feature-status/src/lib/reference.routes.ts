import { Routes } from '@angular/router';
import { ReferenceStatusPageComponent } from './reference-status-page.component';

export const referenceRoutes: Routes = [
  { path: '', component: ReferenceStatusPageComponent },
];
